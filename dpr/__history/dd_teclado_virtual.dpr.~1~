program dd_teclado_virtual;

uses
  Forms,
  uniSmKeyboard in '..\..\ddkeyboard old\uniSmKeyboard.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
